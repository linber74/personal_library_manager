package model;

public class Game extends LibraryItem {

    public Game(String title, String genre, String language, String format, boolean seriesOrNot, String serieName, String serieNumber) {
        super(title, genre, language, format, seriesOrNot, serieName, serieNumber);
    }
}
