package MediaArchive.Games;

import MediaArchive.Archive;

public class Game extends Archive {

    public Game(String title, String genre, String language, String format) {
        super(title, genre, language, format);
    }
}
